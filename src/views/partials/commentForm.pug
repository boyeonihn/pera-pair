form.commentForm(data-login=`${ loggedIn ? loggedInUser._id : ''}`, data-name=`${ loggedIn ? loggedInUser.name : ''}` data-avatar=`${ loggedIn ? loggedInUser.avatarUrl : ''}`)
    textarea(spellcheck="true" placeholder='Write a nice comment...')
    div.button__container
        button.add__comment Add Comment
        button(type="button").cancel__comment Clear