mixin post(post)
    div.mixin__data.mixin__data__post
        a(href=`/posts/${post.id}`)
            span.mixin__title=post.title
        div.mixin__meta
            span
                a(href=`/users/${post.owner._id}`) @#{post.owner.name} • 
            span #{post.meta.views} views
            span.mixin__post__topic
                a(href=`/posts/tag/${post.topic}`) #{post.topic}
            if post.meta.comments == 0 
                span.mixin__post__comments No comments (yet!)
            else
                span.mixin__post__comments #{post.meta.comments} comments