extends base
include mixins/video

block content
    form(method="GET")
        input(placeholder="Search by Title", name="keyword", type="text")
        input(type="submit", value="Search Now")

    div
        if videos 
            h2 Results
            each video in videos
                +video(video)
        else
            h3 Sorry, nothing found with that search query.